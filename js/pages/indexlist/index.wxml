<view class="container">
  <ui-tree wx:if="{{indexList}}" list="{{indexList}}" class="tab-boxer" />
  <ui-list wx:if="{{touchbar}}" list="{{touchbar}}" />
</view>
<!-- <ui-item item="{{codeButton}}" />
<ui-pop item="{{readme}}" /> -->