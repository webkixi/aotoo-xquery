<view class="container">
  <view style="width: 100%;height: 40px; font-size: 24px; text-align: center;margin-bottom: 1.5em;">获取图片主色调</view>
  <ui-list list="{{uploaderConfig}}" />
  <ui-item item="{{demoConfig}}" />
</view>
<ui-item item="{{codeButton}}" />
<ui-pop item="{{readme}}" />